import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './index.css'

// strict mode was been removed due to stricter strict mode (React 18)
ReactDOM.createRoot(document.getElementById('root')).render(<App />)
